# Medical Atlas

**Medical Atlas** е уеб базирана система за подпомагане на клиничното решение при антибиотикорезистентност чрез машинно обучение. Платформата позволява на лекари да въвеждат клинични данни и да получават вероятностна прогноза за резистентност към антибиотик.

---

## Основна идея

Антибиотикорезистентността е глобален проблем. Неправилният избор на антибиотик може да доведе до усложнения и неуспешно лечение.

Medical Atlas интегрира:

*  Анализ на исторически данни
*  Машинно обучение 
*  Модерен уеб интерфейс
*  Сигурна автентикация

Системата подпомага, но **не заменя** клиничното решение.

---

##  Архитектура

Тристепенна архитектура:

```
Браузър (Next.js Frontend)
        ↓
Next.js API Routes
        ↓
FastAPI Backend
        ↓
PostgreSQL + ML модел (CatBoost)
```

### Технологии

###  Frontend

* Next.js 16
* React 19
* TypeScript
* Tailwind CSS
* Recharts
* React Hook Form + Zod

### ⚙ Backend

* FastAPI
* Uvicorn
* Pydantic
* PyJWT
* bcrypt
* psycopg2

###  Machine Learning

* CatBoost Classifier
* KFold Cross Validation
* Feature Engineering
* Target Encoding
* Метрики: Accuracy, F1-score

###  Database

* PostgreSQL
* Разделение на схеми 

###  Deployment

* VPS (Linux)
* Nginx (reverse proxy)
* SSL (Certbot)
* systemd services
* Git LFS (за ML модела)

---

##  Основни функционалности

###  Вход за лекари

* Докторски ID
* Парола
* Secure HTTP-only cookie
* Role-based access control

### Табло (Dashboard)

* Брой проверки за седмицата
* Процент резистентност
* Най-често използвани антибиотици
* Средна възраст на пациентите

###  Прогноза

Многостъпкова форма:

**Стъпка 1:**

* Възраст
* Тегло
* Пол
* Тип заболяване
* Регион

**Стъпка 2:**

* Патоген
* Антибиотик
* Продължителност на лечение

 Резултат:

* Вероятно резистентен / Нерезистентен
* Процент увереност (probability score)

###  Административен панел

* CRUD операции за лекари
* Управление на болници
* Достъп само за роля „Администратор“

---

##  Инсталация (Локално)

### База данни

Инсталирай PostgreSQL и създай база данни:

```sql
CREATE DATABASE medical_atlas;
```

---

### Backend

```bash
cd backend
python -m venv venv
source venv/bin/activate   # или venv\Scripts\activate (Windows)
pip install -r requirements.txt
```

Създай `.env` файл:

```
POSTGRES_HOST=localhost
POSTGRES_DB=medical_atlas
POSTGRES_USER=postgres
POSTGRES_PASSWORD=yourpassword
JWT_SECRET=your_secret_key
```

Стартиране:

```bash
uvicorn src.api:app --host 127.0.0.1 --port 8000 --reload
```

---

### Frontend

```bash
cd frontend
npm install
npm run dev
```

Отвори:

```
http://localhost:3000
```

---

## Production Deployment (VPS)

1. Клониране:

```bash
git clone https://github.com/Di-Boss/Medical-Atlas.git
git lfs pull
```

2. Backend:

```bash
pip install -r requirements.txt
```

3. Frontend:

```bash
npm ci
npm run build
```

4. Конфигурация:

* Nginx reverse proxy
* SSL сертификат (Certbot)
* systemd услуги за API и Web

---

## Сигурност

* HTTPS криптиране
* JWT автентикация
* bcrypt hashing
* Role-based access
* Middleware защита на /admin
* Secure & HTTP-only cookies

---

## Машинно обучение

Моделът използва CatBoost за работа с таблични и категорийни данни.

### Използвани техники:

* Feature Engineering
* Target Encoding
* KFold Cross Validation
* Probability-based classification

### Output:

```json
{
  "resistant": 1,
  "probability": 0.87
}
```

---

## Тестване

* Ръчно тестване на API endpoints
* Валидация на ML резултати
* Проверка на role protection
* Production тестове след деплой


## ⚠ Важно

Medical Atlas е помощен инструмент.
Крайното медицинско решение принадлежи на квалифициран медицински специалист.

---

## 👨‍💻 Автор
Марин Тончев
